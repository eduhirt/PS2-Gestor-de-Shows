extends layout

block content
  .back_img
    //img(src='images/back.jpg', alt='')
  .home-page
    h1.home Lista de eventos
    
    #main
      .inner
        // Boxes
        .thumbnails
          each show in shows
            .box
              a.image.fit(href="/show/#{show.id}") 
                img(src='/images/pic05.jpg', alt='pic')
              .inner
                h3 #{show.name}
                p
                  | #{show.description}
                a.button.fit(href="/show/#{show.id}") Veja Mais



